using Microsoft.Maui.Controls;

namespace Keyboard
{
    public class CustomNoKeyboardEntry : Entry
    {
        // You can add custom logic here if needed
        // This class is used to identify the Entry that should not show the keyboard
    }
}